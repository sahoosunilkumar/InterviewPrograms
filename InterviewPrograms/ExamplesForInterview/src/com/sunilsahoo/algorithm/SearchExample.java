package com.sunilsahoo.algorithm;

public class SearchExample {
	public static void main(String[] args) {

	}

	/*
	 * Time complexity of this algorithm is ( ). This is because in the worst
	 * case we need to scan the complete array. Space complexity: (1).
	 */
	int unsorteddLinearSearch(int A[], int n, int data) {
		{
			for (int i = 0; i < n; i++) {
				if (A[i] == data)
					;
			}
			return -1;
		}
	}
}
